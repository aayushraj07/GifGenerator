import React from "react";

const Display = props => {
  return (
    <div>
      <ul />
    </div>
  );
};

export default Display;

///First make index and app.js in one file

////then do the props!!
